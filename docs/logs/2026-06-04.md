# Work Log - 2026-06-04

## Summary

Verified the first overnight Render Cron output, refined the Daily briefing hierarchy, shortened podcast length, and improved podcast TTS delivery.

## Completed Work

- Verified production `/daily` includes the new `2026-06-04` briefing.
- Verified production `/admin/status` latest job:
  - Status: `succeeded`
  - Candidates: 50
  - Saved articles: 50
  - AI analyzed: 10
  - Podcast script: Ready
  - Podcast audio: Ready
- Verified production `/daily/2026-06-04`:
  - Daily page exists.
  - Top Ranking table is present.
  - Podcast audio player is present.
  - Audio source points to `2026-06-04.mp3`.
  - No horizontal overflow detected.
- Reworked homepage and dated daily pages to reduce table/card duplication:
  - `Today's Top Ranking` remains the full overview.
  - `Featured analysis` expands only the top three articles.
  - Deep-dive article is shown only if it is not already in the featured set.
- Fixed remaining garbled `Today:` replacement text on homepage and dated daily pages.
- Shortened the daily podcast script target:
  - New target length is approximately 7 minutes.
  - Prompt covers only the top three articles in detail.
  - Prompt requests 1,100-1,400 Traditional Chinese characters.
  - Prompt avoids reading journal names, PMID, DOI, or long disclaimers aloud.
- Added fixed daily podcast opening:
  - Host identity: `主持人林醫生`.
  - Humorous seven-minute nephrology improvement framing.
  - AI-generated / self-judgment disclaimer.
- Updated OpenAI TTS generation:
  - Default TTS voice changed from `alloy` to `marin`.
  - Fixed opening was first tested at 1.85x speed, then reduced to 1.5x for clarity.
  - Main briefing body was tested at 0.95x, then increased to 1.05x after listening feedback.
  - Opening and body use different TTS `instructions`.
  - Markdown section headings are no longer spoken aloud.
- Updated podcast duration estimation to account for the faster fixed opening and slower body narration.

## Validation

- `npm.cmd run build` passed after Daily hierarchy changes.
- Browser automation verified local homepage and `/daily/2026-06-04`.
- Local checks confirmed:
  - Top Ranking table rows: 5.
  - Featured analysis article cards: 3.
  - Audio player remains present.
  - No likely garbled text.
  - No horizontal overflow.
- `python -m py_compile` passed for podcast script, TTS, config, persistence, and podcast style updates.
- Verified TTS segmentation:
  - Fixed opening uses fast speed and playful instructions.
  - Main body uses normal podcast instructions and 0.95x speed.
  - Markdown headings are stripped from speech input.
- Generated a local preview MP3 using `marin`, 1.5x fixed opening, and 0.95x body speed.
- Generated a local preview MP3 using `marin`, 1.5x fixed opening, and 1.05x body speed.
- Checked Render environment:
  - `TTS_VOICE` is unset on Render, so the new code default `marin` will apply.

## Open Items

- Listen to the next generated podcast and decide whether `marin` is less stiff than `alloy`.
- Listen to the 1.5x opening preview and decide whether the fixed opening is clear enough.
- Decide whether the body speed should stay at 1.05x after listening to the next preview or daily run.
- Continue collecting physician feedback on ranking rationale and podcast tone.
- Consider Weekly briefing after the daily loop is stable for a few days.
